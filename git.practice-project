# include <stdio.h>
# include "Concepts Based programs "
int main ()
{
    printf("Result: %d",add ece,eee(3,5)):
    printf("Result: %d",add all subjects (3,5)):
    Printf("Result: %d",add one fail subject (2,3));
    return 0;
}